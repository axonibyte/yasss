Welcome to the **Yasss!** end-to-end environment.

This text is served by `GET /v1/texts/coa` and rendered as markdown by the
call-to-action section, so it doubles as a check that the markdown path works
against the real server. It contains a [link](https://crowdease.com) on purpose:
links inside rendered text are expected to pick up Bulma's primary color.
